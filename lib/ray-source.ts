import { Camera } from "./camera";

// TODO do this refactor?

export class RaySource {

    constructor(camera: Camera, xCount: number, yConut: number, AA: number = 1, jitter: boolean = false) {

    }


}